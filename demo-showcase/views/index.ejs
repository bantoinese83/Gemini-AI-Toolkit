<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <header>
            <h1><i class="fas fa-rocket"></i> Gemini AI SaaS Showcase</h1>
            <p>7 AI-Powered Business Solutions Built with Gemini AI Toolkit</p>
            <p class="subtitle">Explore innovative SaaS applications that leverage Google's Gemini AI</p>
        </header>

        <div class="showcase-grid">
            <% saasApps.forEach(app => { %>
            <div class="saas-card" style="border-left-color: <%= app.color %>">
                <div class="card-header">
                    <i class="<%= app.icon %>" style="color: <%= app.color %>"></i>
                    <h3><%= app.name %></h3>
                    <p class="tagline"><%= app.tagline %></p>
                </div>

                <p class="description"><%= app.description %></p>

                <div class="features">
                    <% app.features.forEach(feature => { %>
                    <span class="feature-tag">
                        <i class="fas fa-check"></i> <%= feature %>
                    </span>
                    <% }) %>
                </div>

                <div class="card-actions">
                    <a href="/demo/<%= app.id %>" class="btn-demo" style="background-color: <%= app.color %>">
                        <i class="fas fa-play"></i> Try Demo
                    </a>
                </div>
            </div>
            <% }) %>
        </div>

        <div class="stats-section">
            <div class="stat">
                <div class="stat-number">7</div>
                <div class="stat-label">SaaS Apps</div>
            </div>
            <div class="stat">
                <div class="stat-number">∞</div>
                <div class="stat-label">Possibilities</div>
            </div>
            <div class="stat">
                <div class="stat-number">1</div>
                <div class="stat-label">API Toolkit</div>
            </div>
        </div>

        <footer>
            <p>Built with ❤️ using Gemini AI Toolkit | Transform your ideas into AI-powered businesses</p>
        </footer>
    </div>

    <script src="/js/app.js"></script>
</body>
</html>
